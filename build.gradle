apply plugin: 'idea'

subprojects {
    apply plugin: 'java'

    repositories {
        mavenCentral()
    }

    group = "com.replash"
    version = "${replashVersion}"
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.1'
}